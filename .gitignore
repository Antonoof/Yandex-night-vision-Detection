.venv
project_env/

__pycache__/
*.pyc

# dataset (downloaded separately, see README) and generated YOLO-format tree
data/
# training/eval outputs (checkpoints, logs, results.json, visualizations)
saved/
# Hydra's own per-run log dir
outputs/
# COCO-pretrained weights ultralytics downloads on first run
*.pt
