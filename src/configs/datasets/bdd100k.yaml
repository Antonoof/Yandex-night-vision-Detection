# root to search for a folder with images/ + val.json (nested dirs are OK)
input_dir: data/bdd100k
# where the YOLO-formatted dataset (symlinks + labels + data.yaml) is written
work_dir: data/bdd_yolo
# null = train on everything; set an int for a quick subsample
max_train_images: null
